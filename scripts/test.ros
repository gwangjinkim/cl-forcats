#!/usr/bin/env ros
;; -*- mode: lisp; -*-
(require :asdf)

;; Load local .asd from repo root
(let* ((root (uiop:getcwd))
       (asd  (merge-pathnames "cl-forcats.asd" root)))
  (asdf:load-asd asd))

(format t "~&[cl-forcats] running tests...~%")

(handler-case
    (progn
      (asdf:test-system :cl-forcats)
      (format t "~&OK~%")
      (uiop:quit 0))
  (error (e)
    (format *error-output* "~&TESTS FAILED: ~a~%" e)
    (uiop:quit 1)))
